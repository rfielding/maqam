use anyhow::Result;

fn main() -> Result<()> {
    eprintln!("embroider renderer placeholder: controlled branch source lives here now");
    eprintln!("next commit should replace this with the fixed-carpet guided redraw renderer");
    Ok(())
}
